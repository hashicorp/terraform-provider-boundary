---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "boundary_credential_library_vault_ldap Resource - terraform-provider-boundary"
subcategory: ""
description: |-
  The credential library for Vault LDAP resource allows you to configure a Boundary credential library for Vault LDAP.
---

# boundary_credential_library_vault_ldap (Resource)

The credential library for Vault LDAP resource allows you to configure a Boundary credential library for Vault LDAP.

## Example Usage

```terraform
resource "boundary_scope" "org" {
  name                     = "organization_one"
  description              = "My first scope!"
  scope_id                 = "global"
  auto_create_admin_role   = true
  auto_create_default_role = true
}

resource "boundary_scope" "project" {
  name                   = "project_one"
  description            = "My first scope!"
  scope_id               = boundary_scope.org.id
  auto_create_admin_role = true
}

resource "boundary_credential_store_vault" "foo" {
  name        = "foo"
  description = "My first Vault credential store!"
  address     = "http://127.0.0.1:8200"      # change to Vault address
  token       = "s.0ufRo6XEGU2jOqnIr7OlFYP5" # change to valid Vault token
  scope_id    = boundary_scope.project.id
}

resource "boundary_credential_library_vault_ldap" "foo-static" {
  name                = "foo"
  description         = "My first static Vault LDAP credential library!"
  credential_store_id = boundary_credential_store_vault.foo.id
  path                = "ldap/static-cred/foo" # change to Vault backend path
}

resource "boundary_credential_library_vault_ldap" "bar-dynamic" {
  name                = "bar"
  description         = "My first dynamic Vault LDAP credential library!"
  credential_store_id = boundary_credential_store_vault.foo.id
  path                = "ldap/creds/bar" # change to Vault backend path
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `credential_store_id` (String) The ID of the credential store that this library belongs to.
- `path` (String) The path in Vault to request credentials from.

### Optional

- `description` (String) The Vault LDAP credential library description.
- `name` (String) The Vault LDAP credential library name. Defaults to the resource name.

### Read-Only

- `id` (String) The ID of the Vault LDAP credential library.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import boundary_credential_library_vault_ldap.foo <my-id>
```
